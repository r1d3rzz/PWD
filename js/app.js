function add(a, b) {
  return a + b;
}

function sub(a, b) {
  if (a > b) {
    return a - b;
  } else {
    return b - a;
  }
}

// Test In JavaScript

for (let i = 0; i < array.length; i++) {
  function show() {
    const element = array[i];
  }
}

function hide() {}
function dialog() {}

var user = { name: "rider", age: 21 };

// Control + G show line number
// Control + Shift + O show function symbol / name

function Test(Test) {
  //
}
